name: Discord Daily Notifications

on:
  schedule:
    - cron: '0 22 * * *'   # JST 07:00
    - cron: '0 6 * * *'    # JST 15:00
    - cron: '0 13 * * *'   # JST 22:00
  workflow_dispatch:

jobs:
  fx-analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install anthropic yfinance python-dotenv requests
      - name: FX分析
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          DISCORD_WEBHOOK_FX: ${{ secrets.DISCORD_WEBHOOK_FX }}
          DISCORD_WEBHOOK_ALERTS: ${{ secrets.DISCORD_WEBHOOK_ALERTS }}
        run: python fx_analysis.py

  instagram-likes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install python-dotenv requests
      - name: 競合リスト更新
        run: python update_competitors.py
      - name: いいね候補送信
        env:
          DISCORD_WEBHOOK_INSTAGRAM: ${{ secrets.DISCORD_WEBHOOK_INSTAGRAM }}
          DISCORD_WEBHOOK_ALERTS: ${{ secrets.DISCORD_WEBHOOK_ALERTS }}
        run: python instagram_likes.py

  utage-schedule:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install python-dotenv requests
      - name: UTAGEスケジュール
        env:
          UTAGE_API_KEY: ${{ secrets.UTAGE_API_KEY }}
          DISCORD_WEBHOOK_UTAGE: ${{ secrets.DISCORD_WEBHOOK_UTAGE }}
          DISCORD_WEBHOOK_ALERTS: ${{ secrets.DISCORD_WEBHOOK_ALERTS }}
        run: python utage_schedule.py
